/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ['@neurodivergent-flow/core', '@neurodivergent-flow/api'],
};

module.exports = nextConfig;
